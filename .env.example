# Token for the internal Casino Kings API (passed as &token= to upstream)
CASINO_API_TOKEN="your-token-here"

# OPTIONAL: protect this MCP server with a bearer token.
# If set, Claude must send `Authorization: Bearer <value>` to call any tool.
# Leave unset for an open server.
# MCP_BEARER_TOKEN=change-me-to-a-long-random-string
