export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 40 }}>
      <h1>Casino Kings MCP</h1>
      <p>
        MCP endpoint: <code>/api/mcp</code>
      </p>
      <p>Add this URL as a custom connector in Claude.</p>
    </main>
  );
}
